description: Pause, reset, analyze. After response, remain structured and deliberate for rest of session.

Stop. Do not propose solutions. Do not modify files. Do not run commands.

First, fill this table (be specific, no hand-waving):
| What was tried | Result | Why it didn't work / what's left |

Below the table, write a section called **Analysis:** with:
1. The single root cause (one sentence)
2. 2-3 options with trade-offs (bullet points, no fluff)
3. Your recommendation and why

Then wait for me to say "continue".

**For the rest of this session:**
- Work one step at a time. Wait for my confirmation after each step.
- If something fails, stop and explain why before trying alternatives.
- If the context is long, re-state relevant facts before acting — don't assume I remember.
- Don't try multiple fixes in one turn. Propose one, justify it, wait for my OK.
- Speed is not a priority. Correctness is. Take as many turns as needed.
- This mode stays active until the session ends or I explicitly say "unwind".
